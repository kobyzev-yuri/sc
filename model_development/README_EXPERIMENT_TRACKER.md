# Система отслеживания экспериментов

Система для ведения истории лучших моделей и параметров с метриками, как это принято в ML.

## Основные возможности

- **Централизованный реестр экспериментов** - все эксперименты регистрируются в `experiments/experiments_registry.json`
- **Автоматическое отслеживание лучших результатов** - система автоматически определяет лучшие эксперименты по разным метрикам
- **Интеграция с dashboard** - лучшие эксперименты автоматически отображаются вверху списка в dashboard
- **Метаданные экспериментов** - сохранение информации о train set, версии агрегации, параметрах модели
- **Сравнение экспериментов** - возможность сравнивать несколько экспериментов по метрикам

## Использование

### Автоматическая регистрация

Эксперименты автоматически регистрируются при:
- Сохранении через `export_to_experiment_format()` в `feature_selection_export.py`
- Сохранении через `export_complete_results()` в `feature_selection_export.py`
- Сохранении через `save_experiment()` в `dashboard.py`
- Запуске `run_feature_selection_analysis()` с указанием `output_dir`

### Ручная регистрация существующих экспериментов

```bash
python -m model_development.experiment_tracker_cli register experiments/my_experiment \
    --train-set results/predictions \
    --aggregation-version current
```

### Просмотр списка экспериментов

```bash
# Все эксперименты, отсортированные по score
python -m model_development.experiment_tracker_cli list

# Топ-10 экспериментов
python -m model_development.experiment_tracker_cli top --n 10

# Сортировка по другой метрике
python -m model_development.experiment_tracker_cli list --sort-by separation
```

### Лучшие эксперименты

```bash
python -m model_development.experiment_tracker_cli best
```

Показывает лучшие эксперименты по:
- **Score** (комплексная оценка)
- **Separation** (разделение классов)
- **Mod позиция** (положение патологических образцов на шкале)
- **Объясненная дисперсия** (explained variance)

### Сравнение экспериментов

```bash
python -m model_development.experiment_tracker_cli compare experiment1 experiment2
```

### Сводный отчет

```bash
python -m model_development.experiment_tracker_cli summary --output experiments_summary.md
```

## Интеграция с Dashboard

В dashboard список экспериментов автоматически загружается из трекера и отображается отсортированным по score (лучшие вверху). Лучшие эксперименты (score > 0.8) помечаются эмодзи 🏆.

При сохранении эксперимента через dashboard он автоматически регистрируется в трекере.

## Формат данных

### Реестр экспериментов (`experiments_registry.json`)

```json
{
  "experiments": [
    {
      "id": "abc123def456",
      "name": "feature_selection_forward",
      "directory": "experiments/feature_selection_forward",
      "timestamp": "2024-01-15T10:30:00",
      "metrics": {
        "score": 0.9234,
        "separation": 0.8567,
        "mean_pc1_norm_mod": 0.9123,
        "explained_variance": 0.7845
      },
      "parameters": {
        "selected_features": ["feature1", "feature2"],
        "n_features": 2,
        "method": "forward",
        "use_relative_features": true
      },
      "metadata": {
        "train_set": "results/predictions",
        "aggregation_version": "current"
      }
    }
  ],
  "best_experiments": {
    "best_score": {...},
    "best_separation": {...},
    "best_mod_position": {...},
    "best_explained_variance": {...}
  }
}
```

## Метаданные экспериментов

Каждый эксперимент сохраняет:
- **train_set** - путь к данным обучения (по умолчанию `results/predictions`)
- **aggregation_version** - версия агрегации признаков (по умолчанию `current`)
- **method** - метод подбора признаков
- **selected_features** - список выбранных признаков
- **metrics** - метрики качества модели

## Будущие улучшения

- Поддержка разных train sets (сейчас фиксированный `results/predictions`)
- Поддержка разных версий агрегации признаков
- Визуализация истории экспериментов
- Автоматическое сравнение с baseline
- Экспорт в форматы MLflow, Weights & Biases

