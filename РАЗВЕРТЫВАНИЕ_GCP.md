# 🚀 Быстрое развертывание в Google Cloud

## Шаг 1: Подготовка

### Установите Google Cloud SDK

```bash
# Linux
curl https://sdk.cloud.google.com | bash
exec -l $SHELL

# Или через пакетный менеджер (Ubuntu/Debian)
sudo apt-get update && sudo apt-get install google-cloud-sdk
```

### Войдите и настройте проект

```bash
# Войдите в аккаунт Google
gcloud auth login

# Установите проект (замените YOUR_PROJECT_ID на ID вашего проекта)
gcloud config set project YOUR_PROJECT_ID
```

## Шаг 2: Развертывание

### Вариант А: Автоматический скрипт (Рекомендуется) ⭐

```bash
cd /mnt/ai/cnn/sc
./deploy_gcp.sh
```

Скрипт автоматически:
- ✅ Проверит настройки
- ✅ Включит необходимые API
- ✅ Соберет и развернет приложение
- ✅ Покажет URL вашего дашборда

### Вариант Б: Вручную через gcloud

```bash
cd /mnt/ai/cnn/sc

# Развертывание в Cloud Run
gcloud run deploy dashboard \
  --source . \
  --platform managed \
  --region us-central1 \
  --allow-unauthenticated \
  --port 8080 \
  --memory 2Gi \
  --cpu 2
```

## Шаг 3: Получение URL

После развертывания вы получите URL вида:
```
https://dashboard-xxxxx-uc.a.run.app
```

Откройте этот URL в браузере - ваш дашборд готов!

## Обновление приложения

После изменений в коде просто запустите скрипт снова:

```bash
./deploy_gcp.sh
```

Или вручную:

```bash
gcloud run deploy dashboard --source . --region us-central1
```

## Просмотр логов

```bash
gcloud run services logs read dashboard --region us-central1 --follow
```

## Удаление сервиса

```bash
gcloud run services delete dashboard --region us-central1
```

## Настройка параметров

Вы можете изменить параметры развертывания, отредактировав скрипт `deploy_gcp.sh` или передав переменные окружения:

```bash
REGION=europe-west1 MEMORY=4Gi CPU=4 ./deploy_gcp.sh
```

## Стоимость

- **Бесплатный tier:** 2 миллиона запросов/месяц
- **Платные тарифы:** ~$15-30/месяц при умеренном использовании

## Проблемы?

1. Проверьте логи: `gcloud run services logs read dashboard --region us-central1`
2. Убедитесь, что проект настроен: `gcloud config get-value project`
3. Проверьте, что API включены: `gcloud services list --enabled`

Подробная документация: [DEPLOYMENT_GCP.md](DEPLOYMENT_GCP.md)

