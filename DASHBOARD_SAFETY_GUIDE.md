# Руководство по безопасной работе с Dashboard

## Ответы на ваши вопросы

### ✅ Q: Можно ли из dashboard выбирать какой-то эксперимент?

**A:** Да! Есть два способа:

1. **Через CLI (рекомендуется):**
   ```bash
   python3 -m scale.feature_selection_versioning_cli export feature_selection_quick
   ```
   Затем запустить dashboard - признаки загрузятся автоматически.

2. **В dashboard (планируется):**
   - В боковой панели будет селектор экспериментов
   - Можно выбрать эксперимент из списка
   - Признаки загрузятся автоматически

### ✅ Q: Если я буду сам выбирать фичи, автосохранение не испортит ли мне исходный экспериментальный конфиг?

**A:** НЕТ! Исходные экспериментальные конфиги **НЕ изменяются**!

**Как это работает:**
- Исходные эксперименты хранятся в `experiments/*/best_features_*.json`
- Эти файлы **НИКОГДА не изменяются** при работе в dashboard
- Изменения сохраняются только в `scale/feature_selection_config_relative.json`
- Dashboard отслеживает источник и сохраняет метаданные

### ✅ Q: Как правильно работать в dashboard, чтобы не испортить ничего?

**A:** Вот безопасный рабочий процесс:

---

## Безопасный рабочий процесс

### Шаг 1: Экспортировать эксперимент в dashboard

```bash
# Экспортировать хороший эксперимент
python3 -m scale.feature_selection_versioning_cli export feature_selection_quick
```

**Что происходит:**
- ✅ Признаки копируются в `scale/feature_selection_config_relative.json`
- ✅ Создается резервная копия предыдущей конфигурации
- ✅ Исходный эксперимент (`experiments/feature_selection_quick/best_features_*.json`) **НЕ изменяется**

### Шаг 2: Запустить dashboard

```bash
streamlit run scale/dashboard.py
```

**В dashboard вы увидите:**
- 📊 Информацию об источнике конфигурации (из какого эксперимента)
- 📈 Метрики исходного эксперимента
- ✅ Статус: изменено пользователем или нет

### Шаг 3: Работать с признаками

**Можно безопасно:**
- ✅ Выбирать/снимать признаки через чекбоксы
- ✅ Нажимать "Сохранить конфигурацию"
- ✅ Изменения сохраняются только в `feature_selection_config_relative.json`
- ✅ Исходный эксперимент остается нетронутым

**Что происходит при сохранении:**
- ✅ Изменения сохраняются в `scale/feature_selection_config_relative.json`
- ✅ Сохраняется информация об исходном эксперименте
- ✅ Добавляется флаг `user_modified: true`
- ✅ Исходный эксперимент (`experiments/*/best_features_*.json`) **НЕ изменяется**

### Шаг 4: Вернуться к исходной конфигурации (если нужно)

**Вариант 1: Экспортировать эксперимент заново**
```bash
python3 -m scale.feature_selection_versioning_cli export feature_selection_quick
```

**Вариант 2: Использовать резервную копию**
```bash
# Найти последнюю резервную копию
ls -lt scale/feature_selection_config_relative_backup_*.json | head -1

# Восстановить
cp scale/feature_selection_config_relative_backup_TIMESTAMP.json \
   scale/feature_selection_config_relative.json
```

---

## Что защищено

### ✅ Исходные эксперименты защищены:

```
experiments/
└── feature_selection_quick/
    └── best_features_*.json  ← НИКОГДА не изменяется!
```

Эти файлы **НЕ изменяются** при:
- Работе в dashboard
- Сохранении конфигурации
- Изменении признаков
- Экспорте в dashboard

### ✅ Изменения сохраняются отдельно:

```
scale/
└── feature_selection_config_relative.json  ← Только этот файл изменяется
```

Этот файл:
- Может быть изменен пользователем в dashboard
- Сохраняет информацию об исходном эксперименте
- Можно восстановить из резервной копии

---

## Примеры безопасной работы

### Пример 1: Использовать эксперимент без изменений

```bash
# 1. Экспортировать эксперимент
python3 -m scale.feature_selection_versioning_cli export feature_selection_quick

# 2. Запустить dashboard
streamlit run scale/dashboard.py

# 3. Использовать как есть - исходный эксперимент не изменяется
```

### Пример 2: Изменить признаки в dashboard

```bash
# 1. Экспортировать эксперимент
python3 -m scale.feature_selection_versioning_cli export feature_selection_quick

# 2. Запустить dashboard
streamlit run scale/dashboard.py

# 3. В dashboard:
#    - Изменить признаки через чекбоксы
#    - Нажать "Сохранить конфигурацию"
#    - Изменения сохраняются в feature_selection_config_relative.json
#    - Исходный эксперимент НЕ изменяется!

# 4. Вернуться к исходной конфигурации (если нужно):
python3 -m scale.feature_selection_versioning_cli export feature_selection_quick
```

### Пример 3: Сравнить разные эксперименты

```bash
# 1. Экспортировать первый эксперимент
python3 -m scale.feature_selection_versioning_cli export feature_selection_quick

# 2. Запустить dashboard, проверить результаты

# 3. Экспортировать второй эксперимент
python3 -m scale.feature_selection_versioning_cli export feature_selection_no_paneth

# 4. Запустить dashboard, сравнить результаты

# 5. Оба эксперимента остались нетронутыми!
```

---

## Что показывает dashboard

### Информация об источнике:

```
📊 Конфигурация из эксперимента: feature_selection_quick
```

или

```
📊 Конфигурация из эксперимента: feature_selection_quick (изменено пользователем)
```

### Метрики исходного эксперимента:

```
📈 Метрики исходного эксперимента:
Score: 3.0783
Separation: 6.7904
Mod (норм. PC1): 0.6800
Объясненная дисперсия: 0.5271
```

---

## Резюме

### ✅ Безопасно:

1. **Экспортировать эксперименты** - исходные файлы не изменяются
2. **Изменять признаки в dashboard** - изменения сохраняются отдельно
3. **Сохранять конфигурацию** - исходный эксперимент не изменяется
4. **Экспериментировать** - все исходные данные защищены

### ⚠️ Помните:

1. **Исходные эксперименты защищены** - файлы `experiments/*/best_features_*.json` не изменяются
2. **Изменения сохраняются отдельно** - только в `feature_selection_config_relative.json`
3. **Можно вернуться к исходной конфигурации** - через повторный экспорт или резервную копию

---

## Дополнительная информация

- **Документация по версионированию:** `docs/FEATURE_SELECTION_VERSIONING.md`
- **Управление экспериментами:** `docs/DASHBOARD_EXPERIMENT_MANAGEMENT.md`
- **CLI утилита:** `python3 -m scale.feature_selection_versioning_cli --help`


