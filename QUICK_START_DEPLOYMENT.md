# Быстрый старт - Развертывание Dashboard

## 🎯 Что было создано

1. **Минимальное Dashboard приложение** (`dashboard_minimal.py`)
   - Упрощенная версия с базовым функционалом
   - Загрузка JSON файлов
   - Визуализация и анализ данных

2. **Docker конфигурация**
   - `Dockerfile.dashboard` - оптимизированный для GCP
   - `docker-compose.yml` - для локальной разработки
   - `.dockerignore` - исключения для сборки

3. **Google Cloud конфигурация**
   - `cloudbuild.yaml` - автоматическая сборка и деплой
   - `.gcloudignore` - исключения для gcloud
   - Скрипты в `scripts/` для автоматизации

4. **Инструменты**
   - `scripts/setup_gcp.sh` - настройка GCP проекта
   - `scripts/deploy_gcp.sh` - развертывание в Cloud Run
   - `scripts/build_docker.sh` - локальная сборка
   - `scripts/run_docker.sh` - запуск Docker контейнера

## 🚀 Три способа запуска

### 1. Локально с Docker Compose (самый простой)

```bash
docker-compose up
```

Откройте: http://localhost:8080

### 2. Локально с Docker

```bash
./scripts/build_docker.sh
./scripts/run_docker.sh
```

### 3. В Google Cloud Run

```bash
# Первый раз: настройка
./scripts/setup_gcp.sh

# Развертывание
./scripts/deploy_gcp.sh
```

## 📋 Минимальные требования

- Docker (для локального запуска)
- gcloud CLI (для деплоя в GCP)
- Google Cloud проект с billing

## 📝 Следующие шаги

1. Протестируйте локально: `docker-compose up`
2. Настройте GCP: `./scripts/setup_gcp.sh`
3. Разверните: `./scripts/deploy_gcp.sh`

Подробная документация: `README_DEPLOYMENT_RU.md`

