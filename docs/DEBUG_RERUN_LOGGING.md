# Детальное логирование для диагностики rerun

## ✅ Добавлено детальное логирование

### 1. **Логирование ключей кэширования**

При `DEBUG_MODE = True` теперь выводятся:
- `spectral_settings_key` - текущий ключ настроек
- `features_hash` - хэш признаков
- `features_count` - количество признаков
- Сравнение сохраненного и текущего ключа

### 2. **Логирование причин переобучения**

Выводится подробная информация о том, почему происходит переобучение:
- Нет анализатора
- Нет ключа настроек
- Ключ не совпадает (с показом обоих ключей)
- Признаки изменены

### 3. **Логирование использования кэша**

При использовании кэшированного анализатора:
- Показывается, что ключ совпадает
- Показывается, что используется кэш

При пересчете спектра:
- Показывается причина пересчета
- Показывается текущий и сохраненный ключ

---

## 🔍 Как использовать логирование

1. **Включите DEBUG_MODE:**
   ```python
   DEBUG_MODE = True  # Уже включен
   ```

2. **Откройте dashboard и выберите эксперимент**

3. **Смотрите сообщения DEBUG:**
   - `🔍 DEBUG: spectral_settings_key = ...` - текущий ключ
   - `🔍 DEBUG: Переобучение необходимо: ...` - почему переобучение
   - `🔍 DEBUG: Ключ не совпадает! ...` - проблема с ключом
   - `🔍 DEBUG: Используем сохраненный спектр` - используется кэш

---

## 📝 Анализ логов

### Если видите постоянные rerun:

1. **Проверьте ключи:**
   - Ключ должен быть одинаковым между rerun
   - Если ключ меняется - найдите причину

2. **Проверьте причины переобучения:**
   - Если постоянно "ключ не совпадает" - проблема в генерации ключа
   - Если постоянно "нет анализатора" - проблема в сохранении

3. **Проверьте использование кэша:**
   - Если спектр постоянно пересчитывается - проблема в ключе спектра

---

## ⚠️ Важно

Все логи выводятся через `st.info()`, `st.warning()`, `st.success()` или `st.error()` для видимости в интерфейсе.

Для отключения логирования установите:
```python
DEBUG_MODE = False
```

