# Быстрый старт: Как сохранить хороший результат

## Проблема решена! ✅

Теперь результаты **НЕ перезаписываются автоматически**. Каждый эксперимент сохраняется отдельно.

---

## Как сохранить текущий хороший результат

### Шаг 1: Экспортировать текущий результат в dashboard

Если у вас уже есть хороший результат (например, из `experiments/feature_selection_quick/`):

```bash
# Экспортировать конкретный эксперимент в dashboard
python3 -m scale.feature_selection_versioning_cli export feature_selection_quick
```

**Что происходит:**
- ✅ Создается резервная копия текущей конфигурации dashboard
- ✅ Конфигурация dashboard обновляется признаками из эксперимента
- ✅ При следующем запуске dashboard будут использованы эти признаки

### Шаг 2: Запустить новый тест (безопасно!)

```bash
# Запустить новый тест - результаты сохранятся отдельно
python3 test_feature_selection_quick.py results/predictions
# или
python3 test_without_paneth.py results/predictions
```

**Важно:** Dashboard НЕ будет обновлен автоматически!

### Шаг 3: Сравнить результаты

```bash
# Сравнить два эксперимента
python3 -m scale.feature_selection_versioning_cli compare \
    feature_selection_quick \
    feature_selection_no_paneth
```

### Шаг 4: Выбрать лучший и экспортировать

```bash
# Найти лучший эксперимент
python3 -m scale.feature_selection_versioning_cli best

# Экспортировать лучший в dashboard
python3 -m scale.feature_selection_versioning_cli best --export
```

---

## Пример: Сохранение текущего хорошего результата

У вас есть хороший результат в `experiments/feature_selection_quick/`:

```bash
# 1. Экспортировать его в dashboard (с резервной копией)
python3 -m scale.feature_selection_versioning_cli export feature_selection_quick

# 2. Теперь можно безопасно запускать новые тесты
python3 test_without_paneth.py results/predictions

# 3. Новые результаты сохранятся в experiments/feature_selection_no_paneth/
#    Dashboard останется с предыдущими признаками!

# 4. Если новый результат лучше - экспортировать его
python3 -m scale.feature_selection_versioning_cli export feature_selection_no_paneth

# 5. Если новый результат хуже - ничего не делать, dashboard останется с хорошим результатом
```

---

## Резервные копии

При каждом экспорте создается резервная копия:

```
scale/feature_selection_config_relative_backup_TIMESTAMP.json
```

Чтобы вернуться к предыдущей версии:

```bash
# Найти последнюю резервную копию
ls -lt scale/feature_selection_config_relative_backup_*.json | head -1

# Восстановить (замените TIMESTAMP на реальную дату)
cp scale/feature_selection_config_relative_backup_TIMESTAMP.json \
   scale/feature_selection_config_relative.json
```

---

## Все команды

```bash
# Показать список экспериментов
python3 -m scale.feature_selection_versioning_cli list

# Сравнить эксперименты
python3 -m scale.feature_selection_versioning_cli compare exp1 exp2

# Экспортировать эксперимент в dashboard
python3 -m scale.feature_selection_versioning_cli export experiment_name

# Найти лучший эксперимент
python3 -m scale.feature_selection_versioning_cli best

# Найти лучший и экспортировать
python3 -m scale.feature_selection_versioning_cli best --export
```

---

## Важно!

✅ **Результаты НЕ перезаписываются автоматически**
✅ **Каждый эксперимент сохраняется отдельно**
✅ **Dashboard обновляется только при явном экспорте**
✅ **Создаются резервные копии перед экспортом**

Теперь можно безопасно экспериментировать! 🎉


