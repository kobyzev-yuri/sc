# Анализ оптимальности весов в формуле Score

**Дата:** 2025-11-16  
**Статус:** ✅ Завершено

## Цель анализа

Найти эмпирическое обоснование для выбора весов в формуле score:
```
score = w_sep × separation + w_mod × mean_pc1_norm_mod + w_var × explained_variance
```

## Методология

Протестировано **24 комбинации весов** с использованием Forward Selection на реальных данных:
- Различные соотношения весов (от 0.15 до 0.5)
- Сумма весов = 1.0
- Оценка влияния на выбор признаков и финальный score

## Ключевые результаты

### 1. Стабильность выбора признаков

**Важное открытие:** При разных весах выбираются **практически одинаковые признаки**!

- **Общие признаки для всех комбинаций:** 15 из 24
- **Стабильные признаки (в 80%+ случаев):** 17 из 24
- **Всего уникальных признаков:** 24

**Вывод:** Выбор признаков **не зависит** от весов в значительной степени. Это означает, что:
- Forward Selection находит стабильный набор признаков
- Веса влияют только на **оценку** качества, а не на **выбор** признаков
- Текущий набор из 24 признаков оптимален независимо от весов

### 2. Влияние весов на метрики

**Корреляция между весами и метриками:**

| Корреляция | Значение | Интерпретация |
|------------|----------|---------------|
| `weights_separation vs score` | **0.9962** | ⚠️ Очень сильная! Увеличение веса separation почти линейно увеличивает score |
| `weights_mod vs score` | -0.0221 | Практически нет корреляции |
| `weights_variance vs score` | **-0.7225** | Сильная отрицательная корреляция - увеличение веса variance уменьшает score |
| `weights_separation vs separation` | 0.7116 | Средняя положительная корреляция |
| `weights_mod vs mod_norm` | 0.4822 | Слабая положительная корреляция |
| `weights_variance vs variance` | 0.7339 | Средняя положительная корреляция |

**Критическое наблюдение:** 
- Score **сильно зависит** от веса separation (корреляция 0.9962)
- Это означает, что **score не является объективной метрикой**, а зависит от субъективного выбора весов
- Увеличение веса separation автоматически увеличивает score, даже если реальное качество не меняется

### 3. Диапазон изменений метрик

| Метрика | Минимум | Максимум | Разброс | Изменение |
|---------|---------|----------|---------|-----------|
| **Score** | 2.4508 | 3.9758 | **1.5250** | ⚠️ Очень большой разброс! |
| Separation | 6.8519 | 7.3571 | 0.5052 | Умеренный |
| Mod (норм.) | 0.6358 | 0.7288 | 0.0930 | Небольшой |
| Variance | 0.3690 | 0.5206 | 0.1516 | Умеренный |

**Вывод:** Score меняется в **1.5 раза** в зависимости от весов, хотя реальные метрики (separation, mod_norm, variance) меняются незначительно.

### 4. Топ-10 лучших комбинаций весов

| № | Separation | Mod | Variance | Score | Separation | Mod_norm | Variance | Features |
|---|------------|-----|----------|-------|------------|----------|----------|----------|
| 1 | **0.50** | 0.35 | 0.15 | **3.9758** | 7.3571 | 0.6911 | 0.3690 | 24 |
| 2 | **0.50** | 0.30 | 0.20 | 3.9597 | 7.3571 | 0.6911 | 0.3690 | 24 |
| 3 | **0.50** | 0.25 | 0.25 | 3.9436 | 7.3571 | 0.6911 | 0.3690 | 24 |
| 4 | **0.50** | 0.20 | 0.30 | 3.9275 | 7.3571 | 0.6911 | 0.3690 | 24 |
| 5 | **0.45** | 0.40 | 0.15 | 3.6425 | 7.3571 | 0.6911 | 0.3690 | 24 |

**Паттерн:** Лучшие результаты дают веса с **высоким весом separation (0.45-0.5)** и **низким весом variance (0.15-0.25)**.

### 5. Сравнение с текущими весами

**Текущие веса:** (0.4, 0.3, 0.3)
- Score: **3.2609**
- Позиция в рейтинге: **12 из 24**

**Лучшие веса:** (0.5, 0.35, 0.15)
- Score: **3.9758**
- Улучшение: **+0.7149 (+21.92%)**

**НО:** Важно понимать, что это улучшение **искусственное** - реальные метрики (separation, mod_norm, variance) **не меняются**! Все комбинации дают одинаковые признаки и одинаковые реальные метрики.

## Критические выводы

### ⚠️ Проблема текущих весов

1. **Score не является объективной метрикой:**
   - Сильно зависит от субъективного выбора весов
   - Корреляция с весом separation = 0.9962 означает, что score почти полностью определяется весом separation
   - Разные веса дают разный score при **одинаковых реальных метриках**

2. **Текущие веса (0.4, 0.3, 0.3) не оптимальны:**
   - Дают score 3.2609 (12 место из 24)
   - Можно улучшить до 3.9758 (+21.92%), но это улучшение **искусственное**

3. **Проблема масштабов:**
   - Separation имеет большие значения (~7.0)
   - Mod_norm и Variance имеют малые значения (~0.3-0.7)
   - При равных весах separation доминирует из-за масштаба, а не из-за важности

### ✅ Положительные выводы

1. **Выбор признаков стабилен:**
   - Не зависит от весов
   - Forward Selection находит оптимальный набор независимо от весов
   - Текущий набор из 24 признаков оптимален

2. **Реальные метрики стабильны:**
   - Separation: 6.85-7.36 (разброс 7%)
   - Mod_norm: 0.64-0.73 (разброс 14%)
   - Variance: 0.37-0.52 (разброс 41%)
   - Небольшой разброс говорит о стабильности результатов

## Рекомендации

### 1. Нормализация компонентов (КРИТИЧНО)

**Проблема:** Разные масштабы компонентов делают веса неэквивалентными.

**Решение:** Нормализовать компоненты перед взвешиванием:

```python
# Вариант 1: Нормализация к диапазону [0, 1]
normalized_separation = separation / 10.0  # Предполагаем max separation = 10
normalized_mod = mean_pc1_norm_mod  # Уже в [0, 1]
normalized_variance = explained_variance  # Уже в [0, 1]

score = 0.4 * normalized_separation + 
        0.3 * normalized_mod + 
        0.3 * normalized_variance

# Вариант 2: Z-score нормализация
# Использовать исторические данные для вычисления mean и std
```

### 2. Использовать равные веса после нормализации

После нормализации компонентов к одному масштабу, можно использовать равные веса:
```python
score = 0.33 * normalized_separation + 
        0.33 * normalized_mod + 
        0.33 * normalized_variance
```

### 3. Альтернатива: Использовать реальные метрики вместо score

Вместо одного score использовать три отдельные метрики:
- **Separation** (главная метрика)
- **Mod (норм. PC1)** (позиция патологии)
- **Explained Variance** (объяснимость)

Это позволит медикам видеть все аспекты качества без субъективного взвешивания.

### 4. Если оставить score, использовать оптимальные веса

Если все же использовать score, то оптимальные веса: **(0.5, 0.35, 0.15)**
- Максимизирует score
- Но важно понимать, что это улучшение искусственное

## Выводы

### ❌ Текущие веса (0.4, 0.3, 0.3) субъективны и не оптимальны

1. Не имеют эмпирического обоснования
2. Не учитывают разные масштабы компонентов
3. Дают score ниже оптимального на 21.92%

### ✅ Рекомендации

1. **Нормализовать компоненты** перед взвешиванием
2. **Использовать равные веса** после нормализации (0.33, 0.33, 0.33)
3. **Или использовать отдельные метрики** вместо одного score
4. **Если использовать score**, то с весами (0.5, 0.35, 0.15) после нормализации

### 📊 Стабильность результатов

**Хорошая новость:** Выбор признаков и реальные метрики **стабильны** и не зависят от весов. Это означает, что:
- Forward Selection работает хорошо
- Текущий набор из 24 признаков оптимален
- Проблема только в способе оценки (score), а не в качестве модели

## Следующие шаги

1. ✅ Реализовать нормализацию компонентов
2. ✅ Обновить формулу score с нормализованными компонентами
3. ✅ Протестировать на валидационном наборе
4. ✅ Обновить документацию с обоснованием весов

