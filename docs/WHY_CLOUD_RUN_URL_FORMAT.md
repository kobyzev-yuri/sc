# Почему появляются URL вида `https://dashboard-xxxxx-uc.a.run.app`?

## 🌐 Формат URL Cloud Run

После деплоймента в Cloud Run появляется URL в формате:

```
https://[SERVICE_NAME]-[RANDOM_HASH]-[REGION_CODE].a.run.app
```

### Пример:
```
https://dashboard-gia5jttcaq-uc.a.run.app
```

---

## 🔍 Разбор компонентов URL

### 1. **`dashboard`** - Имя сервиса

Это имя сервиса, которое вы указываете в `deploy_gcp.sh`:

```bash
SERVICE_NAME=${SERVICE_NAME:-dashboard}  # Строка 37 в deploy_gcp.sh
```

**Можно изменить**, но тогда URL полностью изменится!

### 2. **`gia5jttcaq`** - Случайный хэш

Это **уникальный идентификатор**, который Google Cloud генерирует автоматически при **первом создании** сервиса.

**Важно:**
- ✅ Генерируется **один раз** при первом `gcloud run deploy`
- ✅ **Не меняется** при последующих обновлениях
- ✅ Гарантирует уникальность URL в глобальном масштабе
- ✅ Предотвращает конфликты имен между разными проектами

### 3. **`uc`** - Код региона

Это сокращение региона, где развернут сервис:

```bash
REGION=${REGION:-us-central1}  # Строка 36 в deploy_gcp.sh
```

**Коды регионов:**
- `uc` = `us-central1` (США, центральный регион)
- `ue` = `us-east1` (США, восточный регион)
- `ew` = `europe-west1` (Европа, западный регион)
- `as` = `asia-southeast1` (Азия, юго-восточный регион)
- и т.д.

### 4. **`.a.run.app`** - Домен Cloud Run

Это стандартный домен для всех Cloud Run сервисов.

---

## 🎯 Почему именно такой формат?

### Причины:

1. **Глобальная уникальность**
   - Хэш гарантирует, что URL уникален во всем мире
   - Даже если у двух проектов одинаковое имя сервиса, хэш будет разный

2. **Безопасность**
   - Случайный хэш усложняет угадывание URL
   - Нельзя просто перебрать все возможные имена сервисов

3. **Масштабируемость**
   - Google может распределять сервисы по разным серверам
   - Хэш помогает в маршрутизации и балансировке нагрузки

4. **Совместимость с DNS**
   - Формат соответствует стандартам DNS
   - Легко настроить кастомный домен (если нужно)

---

## 🔄 Что происходит при деплойменте?

### Первый раз (создание сервиса):

```bash
gcloud run deploy dashboard --source . --region us-central1
```

**Что происходит:**
1. Cloud Run создает новый сервис с именем `dashboard`
2. Генерирует случайный хэш (например, `gia5jttcaq`)
3. Создает URL: `https://dashboard-gia5jttcaq-uc.a.run.app`
4. Настраивает DNS записи
5. Возвращает URL вам

### Второй раз (обновление):

```bash
gcloud run deploy dashboard --source . --region us-central1
```

**Что происходит:**
1. Cloud Run находит существующий сервис `dashboard`
2. Использует **тот же хэш** (`gia5jttcaq`)
3. Обновляет код/образ сервиса
4. **URL остается прежним**: `https://dashboard-gia5jttcaq-uc.a.run.app` ✅

---

## ⚠️ Когда URL изменится?

URL изменится **ТОЛЬКО** если:

### 1. Удалить и создать сервис заново:

```bash
# Удаление
gcloud run services delete dashboard --region us-central1

# Создание заново
gcloud run deploy dashboard --source . --region us-central1
# → Новый хэш! Новый URL!
```

### 2. Изменить имя сервиса:

```bash
# В deploy_gcp.sh изменить:
SERVICE_NAME=dashboard-new

# При деплое создастся новый сервис с новым URL
```

### 3. Изменить регион:

```bash
# В deploy_gcp.sh изменить:
REGION=europe-west1

# При деплое создастся сервис в новом регионе с новым URL
```

---

## 💡 Как получить URL после деплоймента?

### Способ 1: Из вывода скрипта

Скрипт `deploy_gcp.sh` автоматически показывает URL:

```bash
./deploy_gcp.sh
# ...
# 🌐 Ваш дашборд доступен по адресу:
#    https://dashboard-gia5jttcaq-uc.a.run.app
```

### Способ 2: Через gcloud CLI

```bash
gcloud run services describe dashboard \
  --region us-central1 \
  --format="value(status.url)"
```

### Способ 3: Через Google Cloud Console

1. Откройте [Cloud Run Console](https://console.cloud.google.com/run)
2. Найдите сервис `dashboard`
3. Скопируйте URL из колонки "URL"

---

## 🔧 Использование URL в коде

Если нужно использовать URL в коде (например, для OAuth redirect URI):

```python
import os

# Определяем, работаем ли на Cloud Run
if os.getenv('K_SERVICE'):
    # На Cloud Run - получаем URL из переменной окружения
    service_url = os.getenv('K_SERVICE_URL', 'https://dashboard-gia5jttcaq-uc.a.run.app')
else:
    # Локальная разработка
    service_url = 'http://localhost:8501'
```

**Переменные окружения Cloud Run:**
- `K_SERVICE` - имя сервиса (`dashboard`)
- `K_REVISION` - имя ревизии
- `K_CONFIGURATION` - имя конфигурации
- `K_SERVICE_URL` - полный URL сервиса (если доступен)

---

## 📝 Итог

- ✅ URL генерируется автоматически при первом создании сервиса
- ✅ Формат: `https://[SERVICE_NAME]-[RANDOM_HASH]-[REGION_CODE].a.run.app`
- ✅ Хэш **не меняется** при обновлениях
- ✅ URL **постоянный** - можно давать пользователям
- ✅ URL изменится только при удалении/пересоздании сервиса или изменении имени/региона

Это стандартное поведение Google Cloud Run для обеспечения уникальности и безопасности URL.

