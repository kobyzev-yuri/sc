# Будущая система управления пользователями

## Планируемые улучшения

### 🔐 Система логинов для разных экспериментаторов

В будущих версиях будет добавлена система управления пользователями для разделения работы разных медиков-экспериментаторов.

---

## Предполагаемая структура

### Директории пользователей:

```
experiments/
├── user_doctor_smith/
│   ├── experiment1/
│   │   ├── best_features_*.json
│   │   ├── aggregated_data_*.csv
│   │   └── ...
│   └── experiment2/
│       └── ...
├── user_doctor_jones/
│   └── experiment1/
│       └── ...
└── shared/
    └── public_experiments/
        └── ...
```

### Персональные конфигурации:

```
configs/
├── user_doctor_smith/
│   ├── feature_selection_config_relative.json
│   └── feature_selection_config_absolute.json
├── user_doctor_jones/
│   ├── feature_selection_config_relative.json
│   └── feature_selection_config_absolute.json
└── default/
    └── feature_selection_config_relative.json
```

### Персональные данные:

```
user_data/
├── user_doctor_smith/
│   └── predictions/
│       └── *.json
├── user_doctor_jones/
│   └── predictions/
│       └── *.json
└── shared/
    └── predictions/
        └── *.json
```

---

## Предполагаемый функционал

### 1. Аутентификация

- Логин/пароль или OAuth
- Сессии пользователей
- Автоматический выход при неактивности

### 2. Изоляция данных

- Каждый пользователь видит только свои данные
- Не может изменить данные других пользователей
- Может делиться результатами при необходимости

### 3. Управление конфигурациями

- Персональные конфигурации признаков
- Общие конфигурации (для команды)
- Версионирование конфигураций

### 4. Управление экспериментами

- Персональные эксперименты
- Общие эксперименты (для команды)
- Публичные эксперименты (для всех)

---

## Текущая реализация (без логинов)

### ✅ Что уже работает:

1. **Загрузка данных из директории:**
   - Медики могут указать любую директорию
   - Загружают свои JSON файлы
   - Полный контроль над данными

2. **Сохранение конфигураций:**
   - Сохранение в `feature_selection_config_relative.json`
   - Автоматическая загрузка при следующем запуске
   - Резервные копии при экспорте

3. **Безопасность:**
   - Исходные эксперименты защищены
   - Изменения сохраняются отдельно
   - Можно вернуться к исходной конфигурации

### ⚠️ Ограничения (без логинов):

1. **Общая конфигурация:**
   - Все пользователи используют одну конфигурацию
   - Изменения одного пользователя видны всем

2. **Общие эксперименты:**
   - Все эксперименты в одной директории
   - Нет разделения по пользователям

3. **Общие данные:**
   - Все пользователи видят одни и те же данные
   - Нет персональных директорий

---

## Подготовка к будущему

### Текущая структура кода:

Код уже подготовлен для будущего добавления логинов:

1. **Модульная структура:**
   - Отдельные функции для загрузки/сохранения конфигов
   - Легко добавить проверку пользователя

2. **Гибкие пути:**
   - Конфигурации загружаются из файлов
   - Легко изменить путь на пользовательский

3. **Session state:**
   - Используется для хранения состояния
   - Легко добавить информацию о пользователе

---

## Миграция (когда добавим логины)

### Шаги миграции:

1. **Создать систему аутентификации:**
   ```python
   # scale/auth.py
   def login(username, password):
       # Проверка учетных данных
       return user_session
   ```

2. **Добавить проверку пользователя:**
   ```python
   # В dashboard.py
   if "user" not in st.session_state:
       st.session_state.user = login_user()
   
   user_config_path = f"configs/{st.session_state.user}/feature_selection_config_relative.json"
   ```

3. **Обновить пути:**
   ```python
   # Заменить жестко заданные пути на пользовательские
   config_file = get_user_config_path(st.session_state.user)
   ```

4. **Мигрировать существующие данные:**
   ```python
   # Создать скрипт миграции
   migrate_existing_configs_to_users()
   ```

---

## Рекомендации для текущей работы

### ✅ Для медиков-экспериментаторов:

1. **Используйте свои директории:**
   - Создавайте отдельные директории для каждого эксперимента
   - Используйте понятные имена

2. **Сохраняйте конфигурации:**
   - Сохраняйте конфигурации после успешных экспериментов
   - Делайте резервные копии важных конфигураций

3. **Документируйте:**
   - Записывайте, какие данные использовались
   - Сохраняйте пути к директориям
   - Документируйте результаты

### ⚠️ Помните:

1. **Общая конфигурация:**
   - Изменения видны всем пользователям
   - Сохраняйте важные конфигурации отдельно

2. **Общие эксперименты:**
   - Все эксперименты в одной директории
   - Используйте понятные имена

3. **Будущая миграция:**
   - При добавлении логинов данные будут разделены
   - Важные конфигурации будут сохранены

---

## Заключение

Текущая система уже поддерживает:

✅ **Загрузку данных из любой директории**

✅ **Сохранение конфигураций**

✅ **Безопасную работу с экспериментами**

В будущем будет добавлено:

🔐 **Система логинов**

👥 **Разделение по пользователям**

🔒 **Изоляция данных**

📊 **Управление командами**

Текущая реализация подготовлена для будущего добавления логинов - код модульный и легко расширяемый!


