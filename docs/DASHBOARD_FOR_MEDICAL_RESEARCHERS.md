# Dashboard для медиков-экспериментаторов

## Обзор

Dashboard предоставляет медикам-экспериментаторам полный контроль над данными и конфигурациями для работы с медицинской шкалой патологии.

---

## Основные возможности

### ✅ Загрузка данных из директории

Медики могут загружать свои данные из любой директории:

1. **Выбрать источник данных:**
   ```
   "Использовать директорию"
   ```

2. **Выбрать директорию:**
   - Предустановленные директории (если есть)
   - Или ввести свой путь к директории

3. **Загрузить JSON файлы:**
   - Dashboard автоматически найдет все JSON файлы
   - Загрузит и агрегирует данные
   - Покажет количество загруженных файлов

### ✅ Сохранение своих конфигураций

Медики могут сохранять свои наборы признаков:

1. **Выбрать признаки:**
   - Использовать чекбоксы для выбора/снятия признаков
   - Или использовать конфигурацию из эксперимента

2. **Сохранить конфигурацию:**
   - Нажать кнопку "Сохранить конфигурацию"
   - Конфигурация сохранится в `scale/cfg/feature_selection_config_relative.json`
   - Можно использовать при следующем запуске

3. **Безопасность:**
   - Исходные эксперименты не изменяются
   - Изменения сохраняются отдельно
   - Можно вернуться к исходной конфигурации

---

## Рабочий процесс для медиков

### Шаг 1: Загрузка данных

```bash
# 1. Подготовить данные
# Поместить JSON файлы с предсказаниями в директорию
mkdir -p my_experiment/predictions
cp *.json my_experiment/predictions/

# 2. Запустить dashboard
streamlit run scale/dashboard.py

# 3. В dashboard:
#    - Выбрать "Использовать директорию"
#    - Ввести путь: my_experiment/predictions
#    - Или выбрать из предустановленных
```

### Шаг 2: Выбор признаков

```bash
# В dashboard:
#    - Выбрать/снять признаки через чекбоксы
#    - Или загрузить конфигурацию из эксперимента
#    - Нажать "Применить признаки"
```

### Шаг 3: Сохранение конфигурации

```bash
# В dashboard:
#    - После выбора признаков
#    - Нажать "Сохранить конфигурацию"
#    - Конфигурация сохранится для следующего использования
```

### Шаг 4: Анализ результатов

```bash
# В dashboard:
#    - Запустить PCA анализ
#    - Посмотреть визуализации
#    - Экспортировать результаты
```

---

## Структура файлов для медиков

### Директория с данными:

```
my_experiment/
└── predictions/
    ├── sample1.json
    ├── sample2.json
    └── ...
```

### Сохраненные конфигурации:

```
scale/
├── cfg/
│   └── scale/cfg/feature_selection_config_relative.json  ← Текущая конфигурация
└── feature_selection_config_relative_backup_*.json  ← Резервные копии
```

---

## Будущие улучшения (планируется)

### 🔐 Логин для разных экспериментаторов

В будущем будет добавлена система логинов:

1. **Разделение по пользователям:**
   ```
   experiments/
   ├── user_doctor_smith/
   │   ├── experiment1/
   │   └── experiment2/
   ├── user_doctor_jones/
   │   └── experiment1/
   └── ...
   ```

2. **Персональные конфигурации:**
   ```
   configs/
   ├── user_doctor_smith/
   │   └── scale/cfg/feature_selection_config_relative.json
   ├── user_doctor_jones/
   │   └── scale/cfg/feature_selection_config_relative.json
   └── ...
   ```

3. **Изоляция данных:**
   - Каждый экспериментатор видит только свои данные
   - Не может изменить данные других
   - Может делиться результатами при необходимости

---

## Рекомендации для медиков

### ✅ Хорошие практики:

1. **Организация данных:**
   - Создавайте отдельные директории для каждого эксперимента
   - Используйте понятные имена файлов
   - Ведите документацию по экспериментам

2. **Сохранение конфигураций:**
   - Сохраняйте конфигурации после успешных экспериментов
   - Используйте резервные копии
   - Документируйте, какие признаки использовались

3. **Воспроизводимость:**
   - Сохраняйте пути к данным
   - Записывайте параметры анализа
   - Экспортируйте результаты для документирования

### ⚠️ Что помнить:

1. **Исходные эксперименты защищены:**
   - Файлы `experiments/*/best_features_*.json` не изменяются
   - Можно безопасно экспериментировать

2. **Изменения сохраняются отдельно:**
   - Только в `scale/cfg/feature_selection_config_relative.json`
   - Можно вернуться к исходной конфигурации

3. **Резервные копии:**
   - Создаются автоматически при экспорте
   - Можно восстановить предыдущую версию

---

## Примеры использования

### Пример 1: Новый эксперимент с собственными данными

```bash
# 1. Подготовить данные
mkdir -p my_research/predictions
cp new_predictions/*.json my_research/predictions/

# 2. Запустить dashboard
streamlit run scale/dashboard.py

# 3. В dashboard:
#    - Выбрать "Использовать директорию"
#    - Ввести: my_research/predictions
#    - Выбрать признаки
#    - Сохранить конфигурацию
#    - Проанализировать результаты
```

### Пример 2: Использование конфигурации из эксперимента

```bash
# 1. Экспортировать эксперимент
python3 -m scale.feature_selection_versioning_cli export feature_selection_quick

# 2. Запустить dashboard
streamlit run scale/dashboard.py

# 3. В dashboard:
#    - Выбрать "Использовать директорию"
#    - Указать свою директорию с данными
#    - Признаки загрузятся из эксперимента
#    - Можно изменить и сохранить свою конфигурацию
```

### Пример 3: Сравнение разных наборов признаков

```bash
# 1. Запустить dashboard
streamlit run scale/dashboard.py

# 2. В dashboard:
#    - Загрузить данные
#    - Выбрать первый набор признаков
#    - Сохранить конфигурацию как config1.json
#    
#    - Выбрать второй набор признаков
#    - Сохранить конфигурацию как config2.json
#    
#    - Сравнить результаты
```

---

## FAQ

### Q: Могу ли я использовать свои данные?

**A:** Да! Просто укажите путь к директории с вашими JSON файлами.

### Q: Сохраняются ли мои конфигурации?

**A:** Да! Конфигурации сохраняются в `scale/cfg/feature_selection_config_relative.json` и загружаются автоматически при следующем запуске.

### Q: Могу ли я изменить конфигурацию из эксперимента?

**A:** Да! Вы можете изменить любую конфигурацию и сохранить свою версию. Исходный эксперимент не изменяется.

### Q: Как вернуться к исходной конфигурации?

**A:** Можно экспортировать эксперимент заново или использовать резервную копию.

### Q: Когда будет добавлен логин?

**A:** Это планируется в будущих версиях. Сейчас все экспериментаторы используют общую конфигурацию.

---

## Заключение

Dashboard предоставляет медикам-экспериментаторам:

✅ **Полный контроль** - загрузка данных из любой директории

✅ **Гибкость** - выбор и сохранение своих конфигураций

✅ **Безопасность** - исходные эксперименты защищены

✅ **Удобство** - интуитивный интерфейс для работы

✅ **Будущее** - готовность к добавлению системы логинов

Медики могут свободно экспериментировать с данными и конфигурациями, не беспокоясь о потере исходных экспериментов!


