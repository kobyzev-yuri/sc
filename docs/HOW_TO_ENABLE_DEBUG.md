# Как включить/выключить режим отладки

## 📍 Расположение флага

Флаг `DEBUG_MODE` находится в файле `scale/dashboard.py` на **строке 42**:

```python
# Настройка логирования для отладки
DEBUG_MODE = True  # Включить для отладки session_state проблем
```

## ✅ Текущее состояние

**DEBUG_MODE уже включен** (`DEBUG_MODE = True`)

## 🔧 Как изменить

### Включить отладку:
```python
DEBUG_MODE = True  # Включить для отладки session_state проблем
```

### Выключить отладку:
```python
DEBUG_MODE = False  # Отключить отладочные сообщения
```

## 📝 Что показывает DEBUG_MODE

При `DEBUG_MODE = True` в интерфейсе Streamlit будут отображаться:

1. **Информация о ключах кэширования:**
   - `🔍 DEBUG: spectral_settings_key = ...`
   - `🔍 DEBUG: features_hash = ...`
   - `🔍 DEBUG: features_count = ...`

2. **Причины переобучения:**
   - `🔍 DEBUG: Переобучение необходимо: нет анализатора`
   - `🔍 DEBUG: Переобучение необходимо: ключ не совпадает`
   - `🔍 DEBUG: Переобучение необходимо: признаки изменены`

3. **Использование кэша:**
   - `🔍 DEBUG: Используем сохраненный спектр`
   - `🔍 DEBUG: Пересчет спектра`

4. **Проблемы с ключами:**
   - `🔍 DEBUG: Ключ не совпадает! Сохраненный: ..., Текущий: ...`
   - `🔍 DEBUG: Ключ совпадает: ...`

## ⚠️ Важно

После изменения `DEBUG_MODE` нужно **перезапустить Streamlit**, чтобы изменения вступили в силу:

```bash
# Остановите текущий процесс Streamlit (Ctrl+C)
# Затем запустите снова:
streamlit run scale/dashboard.py
```

## 🎯 Рекомендации

- **Для отладки:** `DEBUG_MODE = True`
- **Для продакшена:** `DEBUG_MODE = False` (чтобы не засорять интерфейс)

