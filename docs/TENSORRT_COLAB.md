# Использование TensorRT на Google Colab

## ✅ Да, TensorRT возможен на Google Colab!

Google Colab предоставляет бесплатные GPU (обычно T4 или V100) и поддерживает TensorRT. Это отличный вариант для конвертации моделей, если локальная установка не работает.

## Преимущества использования Colab

- ✅ Бесплатный доступ к GPU
- ✅ Предустановленные CUDA и библиотеки
- ✅ Простая установка TensorRT
- ✅ Не нужно настраивать локальное окружение

## Инструкция по использованию

### Шаг 1: Откройте Google Colab

1. Перейдите на https://colab.research.google.com/
2. Создайте новый notebook
3. Включите GPU: Runtime → Change runtime type → Hardware accelerator: GPU

### Шаг 2: Установите TensorRT

```python
# Установка TensorRT через pip (работает на Colab)
!pip install nvidia-pyindex
!pip install nvidia-tensorrt

# Проверка установки
import tensorrt as trt
print(f"TensorRT версия: {trt.__version__}")
```

### Шаг 3: Загрузите ваши модели

```python
# Вариант 1: Загрузить через Google Drive
from google.colab import drive
drive.mount('/content/drive')

# Скопируйте модели в Colab
!cp -r /content/drive/MyDrive/path/to/models /content/models

# Вариант 2: Загрузить напрямую через интерфейс Colab
# Files → Upload → выберите .pt файлы
```

### Шаг 4: Установите зависимости проекта

```python
# Клонируйте репозиторий или загрузите файлы
!git clone https://github.com/your-repo/sc.git
# Или загрузите файлы через интерфейс

# Установите зависимости
!pip install ultralytics torch torchvision
```

### Шаг 5: Конвертируйте модели

```python
import sys
sys.path.append('/content/sc')  # или путь к вашему проекту

from convert_to_tensorrt import convert_all_models
from pathlib import Path

# Конвертируйте все модели
models_dir = Path('/content/models')
convert_all_models(models_dir, batch_size=1, imgsz=640, half=True)
```

### Шаг 6: Скачайте результаты

```python
# Скачайте .engine файлы
from google.colab import files

# Скачайте все .engine файлы
!zip -r tensorrt_engines.zip /content/models/*.engine
files.download('tensorrt_engines.zip')

# Или загрузите в Google Drive
!cp /content/models/*.engine /content/drive/MyDrive/models/
```

## Полный пример скрипта для Colab

```python
# ============================================
# Установка TensorRT на Google Colab
# ============================================

# 1. Установка TensorRT
!pip install nvidia-pyindex -q
!pip install nvidia-tensorrt -q

# 2. Проверка
import tensorrt as trt
print(f"✅ TensorRT установлен, версия: {trt.__version__}")

# 3. Установка Ultralytics
!pip install ultralytics -q

# 4. Загрузка моделей (выберите один вариант)
# Вариант A: Через Google Drive
from google.colab import drive
drive.mount('/content/drive')
!cp -r /content/drive/MyDrive/models /content/models

# Вариант B: Загрузить напрямую (используйте интерфейс Colab)

# 5. Конвертация
from ultralytics import YOLO
from pathlib import Path
import os

models_dir = Path('/content/models')
pt_files = list(models_dir.glob('*.pt'))

print(f"Найдено {len(pt_files)} моделей для конвертации")

for pt_file in pt_files:
    print(f"\nКонвертирую {pt_file.name}...")
    model = YOLO(str(pt_file))
    engine_path = model.export(
        format='engine',
        imgsz=640,
        batch=1,
        half=True,
        verbose=True
    )
    print(f"✅ Создан: {engine_path}")

# 6. Скачивание результатов
!zip -r /content/tensorrt_engines.zip /content/models/*.engine
from google.colab import files
files.download('/content/tensorrt_engines.zip')
```

## Важные замечания

### ⚠️ Ограничения Colab

1. **Время сессии**: Бесплатные сессии ограничены ~12 часами
2. **GPU доступность**: GPU может быть недоступен в пиковые часы
3. **Размер файлов**: Ограничения на размер загружаемых файлов

### 💡 Рекомендации

1. **Сохраняйте результаты**: Всегда скачивайте .engine файлы или сохраняйте в Drive
2. **Используйте Colab Pro**: Если нужно больше ресурсов и стабильности
3. **Проверяйте GPU**: Убедитесь что GPU активен перед конвертацией

### 🔍 Проверка GPU

```python
import torch
print(f"CUDA доступна: {torch.cuda.is_available()}")
print(f"GPU: {torch.cuda.get_device_name(0)}")
print(f"CUDA версия: {torch.version.cuda}")
```

## Альтернатива: Использовать готовые .engine файлы

Если конвертация на Colab прошла успешно, вы можете:

1. Скачать .engine файлы на локальную машину
2. Использовать их с вашим кодом (если GPU совместима)
3. Или продолжать использовать PyTorch модели (они работают отлично)

## Сравнение: Colab vs Локальная установка

| Критерий | Google Colab | Локальная установка |
|---------|--------------|---------------------|
| Установка TensorRT | ✅ Простая | ❌ Сложная |
| GPU доступность | ✅ Бесплатно | ✅ Постоянно |
| Скорость | ✅ Хорошая | ✅ Отличная |
| Ограничения | ⚠️ Время сессии | ✅ Нет |

## Вывод

**Да, TensorRT отлично работает на Google Colab!** Это отличная альтернатива если локальная установка вызывает проблемы. Используйте Colab для конвертации моделей, затем скачайте результаты для использования на вашей системе.




