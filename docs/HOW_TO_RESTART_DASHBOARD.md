# Как перезапустить Dashboard

## 🔄 Способ 1: Перезапуск через терминал (рекомендуется)

### Если Streamlit запущен в терминале:

1. **Найдите терминал, где запущен Streamlit**
2. **Нажмите `Ctrl+C`** для остановки
3. **Запустите снова:**
   ```bash
   streamlit run scale/dashboard.py --server.port 8501 --server.address 0.0.0.0
   ```

### Если Streamlit запущен в фоне:

```bash
# Найти процесс
ps aux | grep streamlit

# Остановить процесс (замените PID на реальный)
kill <PID>

# Запустить снова
streamlit run scale/dashboard.py --server.port 8501 --server.address 0.0.0.0
```

## 🔄 Способ 2: Автоматическая перезагрузка

Streamlit **автоматически перезагружает код** при изменении файлов, но иногда это может не сработать для:
- Изменений в импортах
- Изменений глобальных переменных (как `DEBUG_MODE`)
- Изменений в конфигурации

В этом случае лучше сделать **полный перезапуск** (Способ 1).

## ✅ Проверка, что изменения применились

После перезапуска:

1. **Откройте dashboard в браузере**
2. **Выберите эксперимент**
3. **Проверьте наличие сообщений DEBUG:**
   - Должны появиться сообщения типа `🔍 DEBUG: ...`
   - Если сообщений нет - возможно `DEBUG_MODE = False` или код не перезагрузился

## 🔍 Текущий процесс Streamlit

Ваш Streamlit запущен с PID: **231598**

Для перезапуска:
```bash
# Остановить
kill 231598

# Запустить снова
cd /mnt/ai/cnn/sc
streamlit run scale/dashboard.py --server.port 8501 --server.address 0.0.0.0
```

## ⚠️ Важно

После перезапуска:
- Все сессии пользователей будут разорваны
- `session_state` будет очищен
- Нужно будет заново выбрать эксперимент

Но это нормально для применения изменений в коде.

