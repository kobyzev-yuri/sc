# TensorRT Оптимизация YOLO Моделей

TensorRT - это библиотека NVIDIA для оптимизации глубоких нейронных сетей, которая может значительно ускорить инференс на NVIDIA GPU (обычно в 2-5 раз быстрее чем стандартный PyTorch).

## Требования

1. **NVIDIA GPU** с поддержкой CUDA
2. **CUDA Toolkit** (совместимая версия с вашей GPU)
3. **TensorRT** (установлен и совместим с CUDA и PyTorch)
4. **PyTorch** с CUDA поддержкой

## Установка TensorRT

### ⚠️ Важно: Проблемы с установкой через pip

Пакет `tensorrt-cu12` через pip часто не устанавливается из-за проблем с setup.py. Используйте альтернативные методы ниже.

### Вариант 1: Через NVIDIA PyIndex (рекомендуется для pip)

```bash
# Установка NVIDIA PyIndex
pip install nvidia-pyindex

# Установка TensorRT
pip install nvidia-tensorrt
```

**Примечание:** Это устанавливает предкомпилированные wheel файлы от NVIDIA, что более надежно чем tensorrt-cu12.

### Вариант 2: Через conda (если используете conda)

```bash
conda install -c nvidia tensorrt
```

### Вариант 3: Через официальный пакет NVIDIA (наиболее надежно)

1. Скачайте TensorRT с [NVIDIA Developer](https://developer.nvidia.com/tensorrt)
2. Распакуйте архив
3. Установите Python пакет из распакованной директории:
   ```bash
   cd TensorRT-<version>/python
   pip install tensorrt-*-cp310-linux_x86_64.whl  # для Python 3.10
   ```
4. Установите зависимости:
   ```bash
   pip install numpy
   ```

### Вариант 4: Использование Google Colab (рекомендуется если локальная установка не работает)

**Отличная альтернатива!** Google Colab предоставляет бесплатные GPU и TensorRT устанавливается легко.

📖 **Подробная инструкция:** См. [TENSORRT_COLAB.md](TENSORRT_COLAB.md)

**Быстрый старт:**
1. Откройте https://colab.research.google.com/
2. Включите GPU: Runtime → Change runtime type → GPU
3. Скопируйте и выполните скрипт `convert_to_tensorrt_colab.py`
4. Загрузите модели и конвертируйте
5. Скачайте .engine файлы

### Вариант 5: Работа без TensorRT (рекомендуется если установка не работает)

**Важно:** TensorRT - это оптимизация для ускорения, но **не обязательна**. PyTorch модели работают отлично и без TensorRT.

Если установка TensorRT вызывает проблемы, можно:
1. Использовать PyTorch модели напрямую (они работают хорошо)
2. TensorRT дает ускорение 2-5x, но для большинства случаев PyTorch достаточно быстрый
3. Система автоматически использует PyTorch fallback если TensorRT недоступен

**Рекомендация:** Если установка TensorRT вызывает проблемы, просто используйте PyTorch модели. Они работают отлично и без TensorRT.

### Проверка установки

```python
import tensorrt as trt
print(f"TensorRT version: {trt.__version__}")
```

## Конвертация моделей в TensorRT формат

Используйте скрипт `convert_to_tensorrt.py` для конвертации всех моделей:

```bash
# Конвертировать все модели в директории models/
python convert_to_tensorrt.py

# Конвертировать модели с конкретными параметрами
python convert_to_tensorrt.py --batch-size 32 --imgsz 640

# Конвертировать одну модель
python convert_to_tensorrt.py --model models/nn_det2_data_outputs_meta_train4.pt

# Использовать FP32 вместо FP16 (более точно, но медленнее)
python convert_to_tensorrt.py --no-half
```

### Параметры конвертации

- `--batch-size`: Размер батча для оптимизации (влияет на производительность)
- `--imgsz`: Размер изображения для оптимизации (обычно 640)
- `--no-half`: Использовать FP32 вместо FP16
- `--models-dir`: Директория с моделями (по умолчанию `models/`)

**Важно**: TensorRT engine файлы (.engine) специфичны для конкретной GPU архитектуры и версии TensorRT. Их нужно генерировать на той же системе, где они будут использоваться.

## Использование TensorRT моделей в коде

### Вариант 1: Использовать функцию с параметром

```python
from scale.model_config import create_model_configs

# Создать конфигурации с TensorRT моделями (с fallback на PyTorch если .engine не найден)
model_configs = create_model_configs(use_tensorrt=True, tensorrt_fallback=True)
```

### Вариант 2: Использовать специальную функцию

```python
from scale.model_config import create_tensorrt_model_configs

# Создать конфигурации с TensorRT моделями
model_configs = create_tensorrt_model_configs(fallback_to_pt=True)
```

### Вариант 3: Использовать класс напрямую

```python
from scale.aimodels import YOLOTensorRTModel

model = YOLOTensorRTModel(
    model_path="models/nn_det2_data_outputs_meta_train4.pt",  # Автоматически найдет .engine файл
    min_conf=0.2,
    cls_index_to_name={0: "Meta"},
    fallback_to_pt=True,  # Использовать PyTorch если .engine не найден
)
```

## Поведение при отсутствии TensorRT файлов

Если параметр `fallback_to_pt=True` (по умолчанию), система автоматически использует обычные PyTorch модели если TensorRT engine файлы не найдены. Это позволяет:

- Использовать один и тот же код на системах с TensorRT и без него
- Постепенно конвертировать модели без изменения кода
- Легко переключаться между TensorRT и PyTorch версиями

## Ожидаемое ускорение

TensorRT обычно дает ускорение в **2-5 раз** по сравнению с PyTorch, в зависимости от:

- Архитектуры GPU (новые GPU дают больше ускорения)
- Размера батча (большие батчи дают больше ускорения)
- Использования FP16 vs FP32 (FP16 быстрее, но может быть менее точно)

## Пример использования в полном пайплайне

```python
from scale.model_config import create_tensorrt_model_configs, get_postprocess_settings
from scale.predict import WSIPredictor
from scale.wsi import WSIIterator

# Создать конфигурации с TensorRT
model_configs = create_tensorrt_model_configs(fallback_to_pt=True)
postprocess_settings = get_postprocess_settings()

# Создать WSI итератор
wsi_iterator = WSIIterator("path/to/wsi/file.tiff")

# Создать предиктор с TensorRT моделями
predictor = WSIPredictor(
    wsi_iterator=wsi_iterator,
    model_configs=model_configs,
    postprocess_settings=postprocess_settings,
    overlap_ratio=0.8,
    parallel_subsections=True,
    max_workers=4,
    enable_timing=True,
)

# Выполнить предсказание
results = predictor.predict_first_section()
```

## Отладка

Если возникают проблемы:

1. **Проверьте наличие TensorRT**:
   ```python
   import tensorrt as trt
   print(trt.__version__)
   ```

2. **Проверьте наличие .engine файлов**:
   ```bash
   ls -lh models/*.engine
   ```

3. **Проверьте совместимость версий**:
   - CUDA версия должна совпадать между PyTorch и TensorRT
   - TensorRT версия должна быть совместима с ultralytics

4. **Проверьте логи**:
   - При `fallback_to_pt=True` будут предупреждения о использовании PyTorch вместо TensorRT

## Ограничения

1. **Специфичность для GPU**: .engine файлы работают только на той же GPU архитектуре, на которой были созданы
2. **Версия TensorRT**: .engine файлы могут не работать с другой версией TensorRT
3. **Размер батча**: TensorRT оптимизирован для конкретного размера батча (можно указать при конвертации)
4. **Размер изображения**: TensorRT оптимизирован для конкретного размера изображения

## Рекомендации

1. **Конвертируйте модели на целевой системе**: Генерируйте .engine файлы на той же системе, где будете использовать
2. **Используйте FP16**: FP16 обычно дает хороший баланс между скоростью и точностью
3. **Тестируйте точность**: Убедитесь что TensorRT версия дает те же результаты что и PyTorch версия
4. **Используйте fallback**: Всегда используйте `fallback_to_pt=True` для совместимости

## Сравнение производительности

Для сравнения производительности TensorRT vs PyTorch, можно использовать тот же код с разными конфигурациями:

```python
# PyTorch версия
model_configs_pt = create_model_configs(use_tensorrt=False)

# TensorRT версия
model_configs_trt = create_model_configs(use_tensorrt=True)

# Сравнить время выполнения с enable_timing=True
```

