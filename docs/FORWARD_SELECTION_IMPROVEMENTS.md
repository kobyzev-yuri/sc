# Улучшение Forward Selection: Стратегии и методы

## Почему Forward Selection - лучший метод?

### Логические преимущества

1. **Учитывает взаимодействие признаков**
   - Каждый признак оценивается в контексте уже выбранных
   - Находит синергетические эффекты между признаками
   - MI и другие фильтры оценивают признаки независимо

2. **Оптимизирует целевую метрику**
   - Прямо максимизирует score (separation + mod_norm + variance)
   - Не использует прокси-метрики (как MI)
   - Учитывает специфику задачи (разделение патологии и нормы)

3. **Учитывает специфику PCA**
   - Пересчитывает PCA на каждом шаге
   - Видит, как добавление признака влияет на PC1
   - Оптимизирует для PCA, а не для произвольной модели

4. **Эффективность**
   - Использует меньше признаков, чем Backward (24 vs 30)
   - При этом достигает того же качества (score: 3.2644 vs 3.2635)

### Результаты Фазы 1 подтверждают

| Метод | Score | Separation | Признаков | Эффективность |
|-------|-------|------------|-----------|---------------|
| **Forward** | **3.2644** | 7.3632 | 24 | ✅ Лучший |
| Backward | 3.2635 | 7.4130 | 30 | Хуже (больше признаков) |
| Mutual Info | 2.9617 | 6.5390 | 16 | Хуже (ниже score) |

---

## Стоит ли использовать другие методы?

### ❌ Не стоит использовать отдельно:

1. **Mutual Information** - слишком простой, не учитывает взаимодействие
2. **Positive Loadings** - показал отрицательный результат (score: -0.61)
3. **Lasso/RFE** - слишком агрессивны, теряют важную информацию

### ✅ Стоит использовать в комбинации:

1. **MI → Forward Selection** (двухэтапный отбор)
   - Этап 1: MI фильтрует до топ-20-25 признаков
   - Этап 2: Forward Selection выбирает финальные 15-20
   - Преимущество: скорость MI + точность Forward

2. **Forward + Backward** (пересечение/объединение)
   - Пересечение: только общие признаки (24 признака)
   - Объединение: все признаки из обоих (30 признаков)
   - Преимущество: стабильность (признаки, выбранные обоими методами)

---

## Способы улучшения Forward Selection

### 1. Оптимизация параметра min_improvement

**Текущее значение:** 0.01

**Вариации для тестирования:**

```python
# Более строгий отбор (меньше признаков)
min_improvement = 0.05  # Только значительные улучшения

# Текущий
min_improvement = 0.01  # Умеренный отбор

# Более мягкий отбор (больше признаков)
min_improvement = 0.001  # Даже небольшие улучшения
```

**Ожидаемый эффект:**
- `min_improvement = 0.05` → меньше признаков (15-18), возможно выше score
- `min_improvement = 0.001` → больше признаков (28-32), возможно лучше separation

**Рекомендация:** Протестировать 0.001, 0.005, 0.01, 0.02, 0.05

### 2. Ограничение максимального числа признаков

**Текущее:** без ограничений (до 24 признаков)

**Вариации:**

```python
max_features = 15  # Компактный набор
max_features = 20  # Средний набор
max_features = 25  # Расширенный набор
```

**Ожидаемый эффект:**
- Меньше признаков → проще интерпретация, меньше риск переобучения
- Больше признаков → возможно лучше разделение, но сложнее модель

**Рекомендация:** Протестировать 15, 20, 24 (текущий), 25, 30

### 3. Изменение весов в формуле score

**Текущая формула:**
```python
score = 0.4 × separation + 0.3 × mod_norm + 0.3 × variance
```

**Вариации:**

```python
# Больше внимания разделению
score = 0.5 × separation + 0.3 × mod_norm + 0.2 × variance

# Больше внимания позиции mod образцов
score = 0.3 × separation + 0.5 × mod_norm + 0.2 × variance

# Больше внимания объясненной дисперсии
score = 0.3 × separation + 0.2 × mod_norm + 0.5 × variance
```

**Ожидаемый эффект:**
- Больше веса separation → больше признаков, лучше разделение
- Больше веса mod_norm → признаки, лучше позиционирующие патологию
- Больше веса variance → признаки, лучше объясняющие вариацию

**Рекомендация:** Протестировать разные веса, найти оптимальный баланс

### 4. Двухэтапный отбор: MI → Forward

**Стратегия:**

```python
# Этап 1: MI фильтрация
mi_features = mutual_information_selection(candidate_features, k=25)

# Этап 2: Forward Selection на отфильтрованных
final_features = forward_selection(mi_features, min_improvement=0.01)
```

**Преимущества:**
- Быстрее (меньше признаков для Forward Selection)
- Убирает заведомо неинформативные признаки
- Сохраняет точность Forward Selection

**Ожидаемый эффект:**
- Скорость: в 2-3 раза быстрее
- Качество: сопоставимо с чистым Forward Selection

### 5. Пересечение Forward и Backward

**Стратегия:**

```python
forward_features = forward_selection(candidate_features)
backward_features = backward_elimination(candidate_features)

# Пересечение: только общие признаки
common_features = set(forward_features) & set(backward_features)

# Оценка на пересечении
metrics = evaluate_feature_set(df, common_features, ...)
```

**Преимущества:**
- Стабильность: признаки, выбранные обоими методами
- Меньше признаков: только самые важные
- Меньше риск переобучения

**Ожидаемый эффект:**
- Признаков: ~20-22 (меньше чем Forward)
- Score: возможно немного ниже, но более стабильный

### 6. Ранний стоп по метрикам

**Стратегия:**

```python
# Останавливаться, если достигнуты целевые метрики
if separation > 7.0 and mod_norm > 0.70 and variance > 0.35:
    break  # Достаточно хорошие метрики, можно остановиться
```

**Преимущества:**
- Меньше признаков при достижении целевых метрик
- Быстрее выполнение

### 7. Обратный Forward Selection (Backward из Forward)

**Стратегия:**

```python
# Шаг 1: Forward Selection до 30 признаков
forward_features = forward_selection(candidate_features, max_features=30)

# Шаг 2: Backward Elimination из Forward признаков
final_features = backward_elimination(forward_features, min_features=15)
```

**Преимущества:**
- Комбинирует преимущества обоих методов
- Forward находит хорошие признаки
- Backward убирает избыточные

### 8. Взвешенный Forward Selection

**Стратегия:**

```python
# Приоритизировать признаки по типу
priority_features = ['Mild', 'Moderate', 'Neutrophils', 'EoE']

# Сначала пробовать приоритетные признаки
for feature in sorted(remaining, key=lambda x: priority_score(x)):
    # ...
```

**Преимущества:**
- Учитывает медицинскую важность признаков
- Может улучшить интерпретируемость

---

## Рекомендуемый план улучшений

### Фаза 2.1: Оптимизация параметров Forward Selection

**Приоритет: ВЫСОКИЙ**

1. **Вариации min_improvement:**
   - `fs_min_imp_0001` - min_improvement=0.001
   - `fs_min_imp_005` - min_improvement=0.005
   - `fs_min_imp_01` - min_improvement=0.01 (текущий)
   - `fs_min_imp_02` - min_improvement=0.02
   - `fs_min_imp_05` - min_improvement=0.05

2. **Вариации max_features:**
   - `fs_max_15` - max_features=15
   - `fs_max_20` - max_features=20
   - `fs_max_25` - max_features=25
   - `fs_max_30` - max_features=30

### Фаза 2.2: Комбинированные методы

**Приоритет: СРЕДНИЙ**

1. **MI → Forward:**
   - `mi_then_fs_k20` - MI до 20, затем Forward
   - `mi_then_fs_k25` - MI до 25, затем Forward

2. **Forward + Backward:**
   - `fs_backward_intersection` - пересечение признаков
   - `fs_backward_union` - объединение признаков

3. **Forward → Backward:**
   - `fs_then_backward` - Forward до 30, затем Backward до 20

### Фаза 2.3: Оптимизация весов score

**Приоритет: НИЗКИЙ**

1. **Вариации весов:**
   - `fs_weights_sep50` - separation=0.5
   - `fs_weights_mod50` - mod_norm=0.5
   - `fs_weights_var50` - variance=0.5

---

## Выводы и рекомендации

### ✅ Forward Selection - лучший базовый метод

**Причины:**
1. Логически обоснован (учитывает взаимодействие, оптимизирует целевую метрику)
2. Практически подтвержден (лучший score в Фазе 1)
3. Эффективен (меньше признаков, чем Backward)

### ✅ Стоит использовать комбинированные подходы

**Рекомендуемые комбинации:**
1. **MI → Forward** - для скорости
2. **Forward + Backward (пересечение)** - для стабильности
3. **Forward → Backward** - для оптимизации

### ✅ Основные направления улучшения

1. **Оптимизация параметров** (min_improvement, max_features)
2. **Комбинированные методы** (MI→Forward, Forward+Backward)
3. **Оптимизация весов score** (если нужно сместить акцент)

### 🎯 Приоритет действий

1. **Сначала:** Оптимизация min_improvement и max_features
2. **Затем:** Комбинированные методы (MI→Forward)
3. **Опционально:** Оптимизация весов score

