# Расследование проблемы "плывущего" score

## 🔍 Проблема

При восстановлении признаков из эксперимента score изменяется:
- **Исходный score из эксперимента:** 3.2644
- **После восстановления признаков:** 3.2609
- **Разница:** 0.0035 (0.1%)

## ✅ Найденные причины

### 1. **Порядок признаков влияет на PCA**

**Проблема:** 
- `df[feature_columns]` возвращает признаки в порядке колонок DataFrame, а не в порядке списка `feature_columns`
- Порядок колонок может отличаться между экспериментом и дашбордом
- PCA чувствителен к порядку признаков из-за численной нестабильности

**Исправление:**
```python
# БЫЛО:
X = df[feature_columns].fillna(0).values

# СТАЛО:
sorted_feature_columns = sorted(feature_columns)  # Гарантируем одинаковый порядок
X = df[sorted_feature_columns].fillna(0).values
```

### 2. **PCA без random_state**

**Проблема:**
- PCA может давать разные результаты из-за численной нестабильности
- Особенно при признаках с одинаковой дисперсией

**Исправление:**
```python
# БЫЛО:
pca = PCA(n_components=1)

# СТАЛО:
pca = PCA(n_components=1, random_state=42)  # Гарантируем воспроизводимость
```

### 3. **Разные данные могут использоваться**

**Проблема:**
- В эксперименте: `df_all = aggregate.select_all_feature_columns(df_features)`
- В дашборде: `df_features` может быть отфильтрован или изменен

**Проверка:**
- Убедиться, что используются одни и те же данные из эксперимента
- Проверить, что `df_features` не фильтруется перед вычислением метрик

## 🔧 Внесенные исправления

### 1. Сортировка признаков в `evaluate_feature_set`

```python
# model_development/feature_selection_automated.py
# КРИТИЧНО: Сортируем признаки для стабильности PCA
sorted_feature_columns = sorted(feature_columns)

# Проверяем, что все признаки есть в данных
missing_features = [f for f in sorted_feature_columns if f not in df.columns]
if missing_features:
    raise ValueError(f"Признаки отсутствуют в данных: {missing_features}")

# Используем отсортированные признаки
X = df[sorted_feature_columns].fillna(0).values
```

### 2. Добавлен random_state в PCA

```python
# КРИТИЧНО: Добавляем random_state для воспроизводимости
pca = PCA(n_components=1, random_state=42)
```

### 3. Сортировка признаков в дашборде

```python
# scale/dashboard.py
# КРИТИЧНО: Сортируем признаки для стабильности PCA
sorted_feature_cols = sorted(feature_cols_for_metrics)

current_metrics = evaluate_feature_set(
    df_features,
    sorted_feature_cols,  # Используем отсортированные признаки
    mod_samples,
    normal_samples
)
```

## 📊 Ожидаемый результат

После исправлений:
- ✅ Порядок признаков всегда одинаковый (отсортированный)
- ✅ PCA дает воспроизводимые результаты (random_state=42)
- ✅ Score должен быть идентичен исходному из эксперимента (3.2644)

## 🧪 Как проверить

1. Загрузите эксперимент с score = 3.2644
2. Сбросьте несколько признаков
3. Восстановите те же признаки
4. Score должен быть **точно** 3.2644 (не 3.2609)

## ⚠️ Важные замечания

1. **Порядок признаков критичен** - даже при одинаковых данных разный порядок может дать разные результаты PCA
2. **random_state важен** - без него PCA может давать разные результаты на одинаковых данных
3. **Данные должны быть идентичны** - убедитесь, что используются те же данные из эксперимента

## 🔍 Дополнительные проверки

Если проблема сохранится, проверить:
1. Используются ли одни и те же образцы (mod_samples, normal_samples)
2. Одинаковы ли значения признаков (возможны округления)
3. Используется ли та же версия sklearn (разные версии могут давать разные результаты)

