# Веб-интерфейс Dashboard

## Обзор

Dashboard предоставляет интерактивный веб-интерфейс для анализа патологий WSI (Whole Slide Images) и построения шкалы оценки патологии.

---

## Запуск Dashboard

```bash
streamlit run scale/dashboard.py
```

Dashboard откроется в браузере по адресу `http://localhost:8501`

---

## Основные возможности

### 1. Загрузка данных

Dashboard поддерживает несколько источников данных:

#### Загрузка из директории
1. Выберите **"Использовать директорию"**
2. Укажите путь к директории с JSON файлами предсказаний
3. Dashboard автоматически найдет и загрузит все JSON файлы

#### Загрузка из эксперимента
1. Выберите **"Использовать данные из эксперимента"**
2. Выберите эксперимент из списка
3. Данные и конфигурация признаков загрузятся автоматически

#### Загрузка из Google Drive (если включено)
1. Выберите **"Google Drive"**
2. Авторизуйтесь через Google OAuth
3. Выберите файлы или папку с предсказаниями

#### Загрузка из Google Cloud Storage (если включено)
1. Выберите **"Google Cloud Storage (GCS)"**
2. Укажите bucket и путь к файлам
3. Данные загрузятся автоматически

---

## Вкладки Dashboard

### 🎯 Выбор признаков

**Функции:**
- Просмотр всех доступных признаков
- Выбор/снятие признаков через чекбоксы
- Загрузка конфигурации из эксперимента
- Сохранение текущей конфигурации
- Просмотр метрик качества набора признаков:
  - Score (комплексная метрика)
  - Separation (разделение классов)
  - Mod позиция (положение патологических образцов)
  - Объясненная дисперсия PC1

**Рекомендации:**
- Начните с загрузки конфигурации из эксперимента
- Используйте метрики для оценки качества набора признаков
- Сохраняйте конфигурации после успешных экспериментов

### 📊 Данные

**Функции:**
- Просмотр загруженных данных в таблице
- Фильтрация и сортировка данных
- Экспорт данных в CSV
- Просмотр статистики по данным

### 📈 Распределения

**Функции:**
- Визуализация распределений признаков
- Корреляционная матрица признаков
- Анализ взаимосвязей между признаками
- UMAP визуализация (если включено)

### 🔬 Спектральный анализ

**Функции:**
- Визуализация распределения PC1
- Выявление мод (пиков) в распределении
- Классификация образцов по модам (normal/mild/moderate/severe)
- Настройка процентилей для нормализации

**Параметры:**
- `percentile_low` - нижний процентиль (по умолчанию 1.0)
- `percentile_high` - верхний процентиль (по умолчанию 99.0)

### 🔍 Анализ образцов

**Функции:**
- Детальный анализ конкретных образцов
- Просмотр значений признаков для образца
- Сравнение образцов
- Рекомендации по интерпретации результатов

### 📋 Статистика

**Функции:**
- Статистические тесты (t-test, Mann-Whitney U)
- Сравнение групп образцов (mod vs normal)
- Визуализация статистических различий

### 🔮 Инференс

**Функции:**
- Применение обученной модели к новым данным
- Вычисление шкалы патологии для новых WSI
- Сохранение результатов инференса

**Требования:**
- Обученная модель (SpectralAnalyzer или PCAScorer)
- Новые данные в формате JSON предсказаний

---

## Рабочий процесс

### Базовый рабочий процесс

1. **Загрузка данных:**
   - Выберите источник данных (директория, эксперимент, GDrive, GCS)
   - Дождитесь загрузки данных

2. **Выбор признаков:**
   - Перейдите на вкладку "🎯 Выбор признаков"
   - Загрузите конфигурацию из эксперимента или выберите признаки вручную
   - Оцените метрики качества
   - Сохраните конфигурацию

3. **Анализ:**
   - Перейдите на вкладку "🔬 Спектральный анализ"
   - Запустите анализ
   - Просмотрите результаты и визуализации

4. **Сохранение результатов:**
   - Нажмите кнопку "Сохранить эксперимент" в боковой панели
   - Эксперимент будет сохранен в `experiments/experiment_YYYYMMDD_HHMMSS/`

### Работа с экспериментами

1. **Использование существующего эксперимента:**
   - Выберите "Использовать данные из эксперимента"
   - Выберите эксперимент из списка
   - Признаки загрузятся автоматически
   - Исходный эксперимент НЕ изменяется

2. **Изменение признаков:**
   - Выберите/снимите признаки через чекбоксы
   - Нажмите "Сохранить конфигурацию"
   - Изменения сохраняются в `scale/cfg/feature_selection_config_relative.json`
   - Исходный эксперимент остается нетронутым

3. **Создание нового эксперимента:**
   - Загрузите данные
   - Выберите признаки
   - Запустите анализ
   - Сохраните эксперимент

---

## Сохранение и загрузка конфигураций

### Сохранение конфигурации признаков

1. Выберите признаки на вкладке "🎯 Выбор признаков"
2. Нажмите кнопку **"Сохранить конфигурацию"**
3. Конфигурация сохранится в `scale/cfg/feature_selection_config_relative.json`

### Загрузка конфигурации из эксперимента

1. На вкладке "🎯 Выбор признаков"
2. Выберите эксперимент из списка
3. Нажмите **"Загрузить из эксперимента"**
4. Признаки загрузятся автоматически

### Восстановление исходной конфигурации

1. Нажмите кнопку **"Восстановить исходную конфигурацию"**
2. Конфигурация вернется к состоянию из исходного эксперимента

---

## Сохранение экспериментов

### Через Dashboard

1. После анализа данных
2. Нажмите кнопку **"Сохранить эксперимент"** в боковой панели
3. Эксперимент будет сохранен с автоматически сгенерированным именем

**Что сохраняется:**
- Результаты анализа (`results.csv`)
- Метаданные (`metadata.json`)
- Обученная модель (`spectral_analyzer.pkl`, если использовалась)
- Конфигурация признаков (`best_features_*.json`)

---

## Безопасность работы

### ✅ Безопасные действия:

1. **Выбор эксперимента:**
   - ✅ Можно выбирать любой эксперимент
   - ✅ Исходный эксперимент не изменяется

2. **Изменение признаков:**
   - ✅ Можно изменять признаки в dashboard
   - ✅ Исходный эксперимент не изменяется
   - ✅ Изменения сохраняются отдельно

3. **Сохранение конфигурации:**
   - ✅ Создается резервная копия перед сохранением
   - ✅ Исходный эксперимент не изменяется

### ⚠️ Что нужно помнить:

1. **Исходные эксперименты защищены:**
   - Файлы `experiments/*/best_features_*.json` не изменяются
   - Можно безопасно экспериментировать

2. **Изменения в dashboard:**
   - Сохраняются только в `scale/cfg/feature_selection_config_relative.json`
   - Можно вернуться к исходной конфигурации

3. **Резервные копии:**
   - Создаются автоматически при экспорте экспериментов
   - Можно восстановить предыдущую версию

---

## Рекомендации

### ✅ Хорошие практики:

1. **Организация данных:**
   - Создавайте отдельные директории для каждого эксперимента
   - Используйте понятные имена файлов
   - Ведите документацию по экспериментам

2. **Сохранение конфигураций:**
   - Сохраняйте конфигурации после успешных экспериментов
   - Используйте резервные копии
   - Документируйте, какие признаки использовались

3. **Воспроизводимость:**
   - Сохраняйте пути к данным
   - Записывайте параметры анализа
   - Экспортируйте результаты для документирования

4. **Работа с экспериментами:**
   - Используйте существующие эксперименты как отправную точку
   - Экспериментируйте с признаками безопасно
   - Сохраняйте новые эксперименты для сравнения

---

## FAQ

### Q: Могу ли я использовать свои данные?

**A:** Да! Просто укажите путь к директории с вашими JSON файлами предсказаний.

### Q: Сохраняются ли мои конфигурации?

**A:** Да! Конфигурации сохраняются в `scale/cfg/feature_selection_config_relative.json` и загружаются автоматически при следующем запуске.

### Q: Могу ли я изменить конфигурацию из эксперимента?

**A:** Да! Вы можете изменить любую конфигурацию и сохранить свою версию. Исходный эксперимент не изменяется.

### Q: Как вернуться к исходной конфигурации?

**A:** Можно восстановить исходную конфигурацию в dashboard или экспортировать эксперимент заново.

### Q: Что делать, если dashboard не загружает данные?

**A:** Проверьте:
- Правильность пути к директории
- Формат JSON файлов (должны соответствовать формату предсказаний)
- Наличие прав доступа к файлам

### Q: Как интерпретировать метрики качества?

**A:** См. раздел "Метрики качества" в документации [МЕТОДЫ_ПОЛУЧЕНИЯ_ШКАЛЫ.md](МЕТОДЫ_ПОЛУЧЕНИЯ_ШКАЛЫ.md)

---

## Заключение

Dashboard предоставляет:

✅ **Полный контроль** - загрузка данных из различных источников  
✅ **Гибкость** - выбор и сохранение своих конфигураций  
✅ **Безопасность** - исходные эксперименты защищены  
✅ **Удобство** - интуитивный интерфейс для работы  
✅ **Визуализация** - графики и таблицы для анализа результатов

Можно безопасно экспериментировать с данными и конфигурациями, не беспокоясь о потере исходных экспериментов!

