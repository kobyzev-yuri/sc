# Веб-интерфейс для сравнения методов построения шкалы

## Запуск

### Локальный запуск:

```bash
cd /mnt/ai/cnn/sc
streamlit run scale/dashboard.py
```

Дашборд откроется в браузере по адресу `http://localhost:8501`

### Запуск для внешнего доступа (демонстрация):

**Вариант 1: Автоматический скрипт (рекомендуется):**
```bash
cd /mnt/ai/cnn/sc
./start_dashboard.sh
```

Скрипт автоматически:
- Запустит Streamlit dashboard
- Запустит ngrok туннель
- Покажет публичный URL для доступа

**Вариант 2: Вручную:**
```bash
# Терминал 1: Запустите dashboard
streamlit run scale/dashboard.py --server.port 8501 --server.address 0.0.0.0

# Терминал 2: Запустите ngrok
ngrok http 8501
```

Подробнее см. [DEPLOYMENT.md](../DEPLOYMENT.md)

## Возможности

### 1. Загрузка данных

- **Из директории**: Включите "Использовать данные из results/predictions" - автоматически загрузит все JSON файлы
- **Через интерфейс**: Загрузите JSON файлы с предсказаниями через файловый загрузчик

### 2. Сравнение методов

В боковой панели включите **"Включить сравнение методов"**, затем выберите методы:

- ✅ **PCA Scoring** - простая нормализация
- ✅ **Spectral Analysis [1, 99]** - спектральный анализ с процентилями 1-99
- ✅ **Spectral Analysis [0.5, 99.5]** - более широкие процентили
- ✅ **Spectral Analysis [5, 95]** - более узкие процентили
- ✅ **Spectral Analysis + GMM** - с Gaussian Mixture Model
- ✅ **Кастомный Spectral Analysis** - настройте свои процентили

### 3. Вкладка "Сравнение методов"

После выбора методов и загрузки данных откройте вкладку **"⚖️ Сравнение методов"**:

#### Что вы увидите:

1. **📊 Статистика по методам**
   - Средние значения, стандартные отклонения
   - Минимум, максимум, медиана
   - Число мод (для spectral analysis)
   - Используемые процентили

2. **📋 Сравнение шкал для каждого образца**
   - Таблица со шкалами всех методов для каждого образца
   - Можно отсортировать и проанализировать различия

3. **📈 Визуализация сравнения**
   - Распределение шкал (гистограммы)
   - Boxplot сравнение методов
   - Scatter plot сравнения двух методов
   - Корреляционная матрица между методами

4. **💾 Скачивание результатов**
   - CSV с сравнением всех методов
   - CSV со статистикой
   - PNG график сравнения

### 4. Сохранение экспериментов

Нажмите кнопку **"Сохранить эксперимент"** в боковой панели:
- Сохранятся все результаты
- Если было сравнение методов - сохранятся и они
- Все файлы сохранятся в `experiments/experiment_YYYYMMDD_HHMMSS/`

## Пример использования

1. Запустите дашборд: `streamlit run scale/dashboard.py`
2. Включите "Использовать данные из results/predictions"
3. Включите "Включить сравнение методов"
4. Выберите несколько методов (например, PCA Scoring и Spectral [1, 99])
5. Перейдите на вкладку "⚖️ Сравнение методов"
6. Изучите результаты, графики и статистику
7. Скачайте результаты или сохраните эксперимент

## Преимущества веб-интерфейса

✅ **Интерактивность** - меняйте параметры и сразу видите результаты  
✅ **Визуализация** - графики прямо в браузере  
✅ **Удобство** - не нужно писать код, все через интерфейс  
✅ **Сравнение** - легко сравнивать несколько методов одновременно  
✅ **Эксперименты** - сохраняйте и загружайте результаты  

## Требования

```bash
pip install streamlit matplotlib seaborn pandas numpy scikit-learn scipy
```


