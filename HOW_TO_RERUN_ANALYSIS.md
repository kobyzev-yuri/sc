# Как перезапустить анализ в веб-интерфейсе

## Автоматический перезапуск в Streamlit

Веб-интерфейс на Streamlit **автоматически перезапускает анализ** при изменении любых настроек!

## Как это работает:

1. **Запустите дашборд:**
   ```bash
   streamlit run scale/dashboard.py
   ```

2. **Измените любую настройку в боковой панели:**
   - ✅ Включите/выключите "Использовать относительные признаки"
   - ✅ Измените процентили (слайдеры)
   - ✅ Включите/выключите "Применить спектральный анализ"
   - ✅ Включите/выключите "Использовать GMM для моделирования состояний"
   - ✅ Измените выбор признаков

3. **Streamlit автоматически перезапустит анализ!**
   - Весь код выполнится заново
   - PCA будет переобучен
   - Спектральный анализ будет пересчитан
   - Все графики обновятся

## Что происходит при перезапуске:

### 1. Агрегация данных (строки 307-350)
```python
# Агрегация данных
with st.spinner("Агрегация данных..."):
    rows = []
    for image_name, preds in predictions.items():
        stats = aggregate.aggregate_predictions_from_dict(preds, image_name)
        rows.append(stats)
    df = pd.DataFrame(rows)
    
    if use_relative_features:
        df_features = aggregate.create_relative_features(df)
        df_features = aggregate.select_feature_columns(df_features)  # ← Здесь используются новые признаки Paneth!
```

### 2. Спектральный анализ (строки 663-686)
```python
if use_spectral_analysis and len(df_features) > 0:
    with st.spinner("Обучение спектрального анализатора..."):
        analyzer = spectral_analysis.SpectralAnalyzer()
        
        # PCA - переобучение с новыми признаками
        analyzer.fit_pca(df_features)
        
        # Преобразование через PCA
        df_pca = analyzer.transform_pca(df_features)
        
        # Анализ спектра - пересчет с новыми параметрами
        analyzer.fit_spectrum(
            df_pca,
            percentile_low=percentile_low,  # ← Используются значения из слайдеров
            percentile_high=percentile_high,
        )
        
        # GMM (если включено)
        if st.checkbox("Использовать GMM для моделирования состояний"):
            analyzer.fit_gmm(df_pca)
        
        # Преобразование в спектральную шкалу
        df_spectrum = analyzer.transform_to_spectrum(df_pca)
```

## После добавления Paneth:

Теперь, когда мы добавили Paneth в `select_feature_columns()`, при перезапуске анализа:

1. ✅ **Paneth_relative_count** будет включен в признаки
2. ✅ **Paneth_relative_area** будет включен в признаки  
3. ✅ **Paneth_mean_relative_area** будет включен в признаки
4. ✅ PCA будет переобучен с учетом Paneth
5. ✅ Спектральный анализ будет пересчитан с новыми признаками

## Как перезапустить анализ с Paneth:

1. Откройте дашборд: `streamlit run scale/dashboard.py`
2. Убедитесь, что включено "Использовать данные из results/predictions"
3. **Просто измените любую настройку** (например, переместите слайдер процентилей)
4. Streamlit автоматически перезапустит весь анализ с новыми признаками Paneth!

## Альтернативные способы перезапуска:

- **Кнопка "Rerun"** в правом верхнем углу Streamlit (или `R` на клавиатуре)
- **Изменение любого виджета** (чекбокс, слайдер, выбор)
- **Обновление страницы** в браузере (F5)

## Проверка, что Paneth включен:

После перезапуска откройте вкладку **"📊 Данные"** и проверьте колонки:
- Должны быть: `Paneth_relative_count`, `Paneth_relative_area`, `Paneth_mean_relative_area`

Или откройте вкладку **"🎯 Выбор признаков"** - Paneth должен быть в списке доступных признаков.



